require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe RoutesController do
  before(:each) do
    @routes_controller = RoutesController.new
  end

  it "should desc" do
    # TODO
  end
end

